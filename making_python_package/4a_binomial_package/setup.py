from setuptools import setup

setup(name='binomial_package',
      version='1.1',
      description='Gaussian and binomial distributions',
      packages=['binomial_package'],
      author='Arslan Ali',
      keywords = ['binomial', 'gaussian', 'distribution', 'udacity'],
      zip_safe=False)